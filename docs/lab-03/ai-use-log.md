# Lab 3 AI Use -- Full Prompt Log

The companion to [`ai-use.md`](./ai-use.md), which carries what section 14
Part 4 asks for: the models used, eight selected prompts, and the reflection.
This file is everything else, and it is here rather than there so the
submitted document stays the length the labsheet means by "concise" while
nothing is thrown away.

The reason for keeping it is the same: a prompt cannot be reconstructed
accurately weeks after it was typed, and the ones that look minor are often
the ones that stopped something going wrong.

---

Every prompt of the sprint so far, the eight selected among them. Kept complete because a
prompt cannot be recovered accurately once the session is over, and because the ones that look
minor are often the ones that stopped something going wrong.

| # | Prompt | What it produced |
| --- | --- | --- |
| 1 | `/grill-with-docs @material/UTF-8_Lab_3_sheet.pdf` | Selected — see ai-use.md §2. |
| 2 | *"ขอภาษาไทยได้มะ"* | Selected — see ai-use.md §2. |
| 3 | Round 1 answers — *"a, crypto.scrypt, a, b, Split by boundary, b, Same cadence"* | Settled hashing, the session store, and how the sprint splits into Issues. `crypto.scrypt` was chosen over an external Argon2 dependency; the peer's repository chose Argon2id, which is how the two specifications came to differ on a point neither of us had to guess about. |
| 4 | Round 2 answers, including *"session เก็บแค่ `{ id, userId, expiresAt, createdAt }`"* | Selected — see ai-use.md §2. |
| 5 | Round 3 answers — *"รับตามร่าง, (ก) เลิกใช้, ตามตาราง และเปลี่ยนรหัสแล้ว, (ก), ตามนั้น และ requesterStorage.ts ลบทิ้งทั้งไฟล์, รหัสผ่าน 8–128 ตัว"* | Settled the status rename, the deletion of the selector, and the password bounds. |
| 6 | Round 4 answers — *"user ทุก role, ก, 1→2→3→(4→5→6)→7→8→9, ใช้ซ้ำทั้งชุด"* | Settled seed scope and the Issue dependency order: three of the nine can run in parallel, the rest are a line. |
| 7 | `/to-spec` | Turned the settled answers into `specification.md`, `api-spec.md`, `tests.md` and `ui-spec.md`. |
| 8 | *"ทำทั้งสองอย่าง"*<br>"Do both." | Answered a question about whether the authorization matrix belongs in the specification or in the API spec by declining the choice. It lives in §5 of the specification and is cited from the API spec. |
| 9 | `/to-tickets` | Nine Issues, #47–#55. |
| 10 | *"กำลังดี, รวม, เก็บตามจริง, รับได้, lab-03 อย่างเดียว"*<br>"About right · combine them · record what is actually true · acceptable · label `lab-03` only." | Five sizing decisions in one line: Issue granularity, whether to split the comment and note work, whether estimates are aspirational, the risk carried by the Lab 2 test rewrite, and the label. |
| 11 | *"5 คำตอบที่ต้องการคำถามคืออะไร"* | Selected — see ai-use.md §2. |
| 12 | `/implement`, twice | Started #45, then #46. |
| 13 | *"ready let agent start"* | Handed over the contract Issue. |
| 14 | *"อัพเดทกฏลง Claude.md ด้วยเรื่อง living document …"* | Selected — see ai-use.md §2. |
| 15 | *"ใส่ label lab-03 ไว้ด้วยนะ"*<br>"Put the `lab-03` label on it too." | Caught a Pull Request opened without the label. Lab 2 learned this the hard way — unlabelled Issues put two labs' cards in one board screenshot. |
| 16 | *"อัพเดท KanBan ด้วย ๆ"* · *"เก็บการ์ด lab2 ได้เลย"*<br>"Update the Kanban too" · "you can archive the Lab 2 cards." | Produced the finding that the board is scriptable — `gh project item-add` and `item-edit` — which the rules had claimed it was not. |
| 17 | *"https://github.com/beambeambeam/toktickit/pull/59 review ให้หน่อย … review เป็น comment ข้อ ๆ แล้วให้กด resolve"*<br>"Review #59 … as one comment per point, and resolve them." | The half of the instruction that was refused is the interesting half. One finding per line: yes, that is how the peer reviews us. Resolving them: no — resolving is the author's move after fixing, and a thread resolved by the reviewer at the moment it is raised is a finding deleted. That is a rule in `CLAUDE.md` now. |
| 18 | *"กด request change ด้วยมะ"*<br>"Press Request changes as well?" | Turned three line comments with no verdict into an actual review. Findings without a verdict leave the author guessing whether they are blocking. |
| 19 | *"อัพเดท Claude.md ด้วยเรื่องการคอมเม้นกับ request change"*<br>"Write the commenting and request-changes practice into CLAUDE.md." | Both of the above became rules, along with the `gh api … /reviews` recipe that posts the line comments and the verdict in one request rather than two. |
| 20 | *"แก้ 12 ข้อที่เหลือได้เลยแล้วก็ตอบเป็น caveman กลับไป"*<br>"Fix the remaining twelve and reply in caveman." | Twelve review findings fixed in one commit, each thread answered with the commit that fixed it. |
| 21 | *"แล้วแก้ไรไปนะล่าสุด"*<br>"So what was it that got changed, again?" | Asked for the account after the fact and got a correction out of it: one of the twelve fixes had left a contradicting row in a second file, in the very commit whose message said it was fixing that contradiction. |
| 22 | *"ทำให้มัน Generic นะไม่ใช่ระบุแค่อันใดอันนึง"* | Selected — see ai-use.md §2. |
| 23 | *"แต่มีจุดหนึ่งที่ผมอยากแก้ที่สุด …"* and *"ไม่เอาดีกว่าลบออกอันล่าสุดเอาแบบเดิม"* | Selected — see ai-use.md §2. |
| 24 | *"ผูก Development panel ได้มะ"* · *"ใช่ กดเอง"*<br>"Can you link it from the Development panel?" · "Right — I clicked it myself." | Established who had actually created a link that the AI had credited to a closing keyword. The keyword had been given credit for a click. |
| 25 | *"npx skills add JuliusBrussee/caveman ลงอันนี้หน่อย"*<br>"Install this one." | Added the skill used for the replies on #56 and for the review of #60. |
| 26 | *"https://github.com/beambeambeam/toktickit/pull/60 ใช้ caveman code-review medium หน่อย"*<br>"Use caveman code-review, medium, on #60." | An 83-file authentication implementation reviewed. Of the nine findings the automated pass produced, six survived being checked against the source and were posted; three did not and were not. |
| 27 | *"https://github.com/beambeambeam/toktickit/pull/60 re-review ล่ะ"*<br>"Re-review #60 now." | Confirmed all six fixed — two of them more thoroughly than asked — and approved. |
| 28 | *"https://github.com/Kiatisakk/toktickit/pull/57 รีวิวล่ะ"* | Selected — see ai-use.md §2. |
| 29 | *"ai_use.md กับ reviewer.md ทำตอนไหนนะ"*<br>"When do `ai-use.md` and `reviewer.md` get done?" | Found that neither existed. The rule requiring them had been written into `CLAUDE.md` four commits earlier and had not been applied to the lab that wrote it. This file and `reviewer.md` are the answer, and the rule now says both are created when a new `<lab>-staging` branch starts. |
| 30 | *"สร้างเลย ใส่ใน PR #57 แล้วก็เพิ่มกฏว่าต้องสร้างทุกครั้งตอนเริ่ม lab-staging ใหม่แล้วก็ฝากเช็คด้วยตกหล่นอะไรไหม"*<br>"Create them, put them in PR #57, add the rule that they are created every time a new lab-staging starts, and check what else has been dropped." | Both files written and backfilled, the rule added — and the audit it asked for found `CLAUDE.md` still announcing Lab 2 as the current sprint, in the file that had just been edited twice that day. |
| 31 | *"47 ทำต่อเลย"*<br>"Carry straight on with 47." | Authentication. The instruction carried no design in it, which meant the specification had to be the design — and it held: hashing, session shape, the ordered inactive check and the three-step password migration were all already decided, so the work was implementation rather than negotiation. |
| 32 | *"เปิด docker แล้ว"*<br>"Docker is up now." | Unblocked the half of the ticket that needed a database, and the first thing it produced was a refusal. The bootstrap script stopped rather than inventing passwords for three IT Staff rows it had no credentials for — rows Lab 2 had left in the demonstration seed under a comment reading *"Lab 3 moves them when authentication gives them a purpose."* The move happened here, a ticket earlier than the staff features that need it, because a `NOT NULL` password column has to hold for every existing row including theirs. Then the migration failed on the test database with the exact error the peer reviewer had predicted on PR #56 — which is the second time this sprint that a finding raised in review turned out to be a real event rather than a caution. |
| 33 | *"บีม review ล่ะ 2 pr เลย"*<br>"Beam has reviewed — both PRs." | Seventeen findings on #59, each checked against the code before anything was changed. The check itself found the worst one had been understated: the build failure the reviewer reported also crashed the seed at runtime, and had been invisible locally because the Prisma client was stale. Eleven findings fixed; four answered with the reason. The upgrade-path fix was verified by building a throwaway database in the Lab 2 shape rather than by reasoning about the migration. |
| 34 | *"continue นึกไว้ด้วยว่า Lab 2 ทำเสดแล้วส่งเรียนร้อยแล้ว"*<br>"Continue — and remember that Lab 2 is finished and already submitted." | Recorded as a standing fact rather than a passing remark, because the work at that moment was editing Lab 2 tests and seeds. It separates two things that looked alike: Lab 2 *code* still changes when Lab 3 requires it, in the Lab 3 Pull Request that needs it; the Lab 2 *submission* is closed and is not reopened or improved. |
| 35 | *"https://github.com/beambeambeam/toktickit/pull/61 รีวิวให้เพื่อนหน่อย"*<br>"Review my friend's PR #61." | A review of the Administrator user-management slice, and the clearest case this sprint of a check changing the finding. The AI expected the peer's manual `LIKE` escaping to be a double-escape bug and was about to say so. It built a throwaway database on the peer's Prisma version instead, found Prisma does not escape wildcards at all, and the "bug" became the thing the review praised. Two further suspicions were dropped the same way before anything was posted. What remained was a real blocker — the PR would close an Issue with two acceptance criteria that nothing tests. |
| 36 | *"วันหลังไม่ต้องเปิด pr แยกค่อยใส่ใน pr ถัดไปแทน ๆ"*<br>"In future, don't open a separate PR — put it in the next PR instead." | Retired a rule rather than following it. `CLAUDE.md` had said post-merge docs get a `docs/<lab>-<topic>` Pull Request of their own; PR #58 was one, recording how #57 ended, and it conflicted with #59 and was closed. The rule now says such docs ride in the next feature branch's Pull Request, and the commit #58 carried is on #48's branch. |
| 37 | *"ทำ issue 18 ต่อเลย"*<br>"Carry on with issue 18." | Issue #18 is Lab 2's My Tickets and has been closed since Lab 2 was submitted. The AI read it, saw it could not be the one meant, and went by the branch already checked out — `feature/authenticated-requester`, which is #48 — saying so before starting. |
| 38 | *"ทำ #48 ต่อเลย"*<br>"Carry on with #48." | The selector deleted. Server identity comes from the session alone, scope is one module with a read fragment and a write fragment, and the Lab 2 suites sign in instead of setting a header. The part the specification had not foreseen was the browser suite: reusing a saved session means no test may ever click Logout, so switching from Requester A to Requester B became two browser contexts rather than one page. |
| 39 | *"เปิด server ให้ลองเทสหน่อย ๆ"* · *"ปิด server ได้แล้วแล้วทำอะไรต่อ"*<br>"Start the servers so I can try it" · "You can stop them — what next?" | A manual test of #48 against the development database, with the seven accounts worth signing in as and the three things worth trying. The answer to "what next" read the dependency list of every open Issue and recommended #53 as the only one not waiting on #48. |
| 40 | *"start #53"* | Administrator user management. The concurrency rule was checked the way this repository's rules ask claims to be checked: API-38 was run once with `FOR UPDATE` removed, both deactivations succeeded, and only then was the passing test trusted to be testing the lock. |
| 41 | *"รอ #60 merge ก่อน"*<br>"Wait for #60 to merge first." | Settled a mistake of the AI's. It had called #53 independent of #48; the server half is, but the route and navigation live in files #48 rewrites. Asked to choose between stacking the branch on #48, duplicating its route guard, or waiting, the author chose waiting — no duplicate code and no borrowed commits, at the cost of #53 closing after #60. |
| 42 | *"https://github.com/Kiatisakk/toktickit/pull/60 บีมรีวิวแล้ว"*<br>"Beam has reviewed PR #60." | Two findings, both confirmed against the code before any change. One exposed a comment that had become false the moment the ticket's own change landed — the status page was documented as calling only the public health endpoint, and it calls two. Each fix got a regression test, and each test was first run against the unfixed source to watch it fail. |
| 43 | *"https://github.com/beambeambeam/toktickit/pull/62 รีวิวให้เพื่อนด้วยเด้อ"*<br>"Review my friend's PR #62 too." | A 47-file review in which the most useful work was the findings not written. A missing tie-break test turned out to be present — every fixture shared one date — and a suspected integer overflow in pagination was run against PostgreSQL and found safe. What remained was sharper for it: the peer had fixed a 403-handling defect on #61, and the new pages repeated the original. |
| 44 | *"beam merge ล่ะ"*<br>"Beam has merged." | The post-merge steps for #60: the Issue closed by hand, the card to Done. And a check on the branch the follow-up docs were meant to ride on, which found it could not carry them yet — #53 had to take in `lab3-staging` first, with conflicts in code as well as documents. That was reported as work still owed rather than done quietly as a side effect. |
| 45 | *"ต่อจากนี้ labels เขียนว่า DO NOT MERGE อย่าพึ่ง merge เขียนไว้ในกฏเลย"*<br>"From now on, a label saying DO NOT MERGE means don't merge yet — write it into the rules." | A standing rule in `CLAUDE.md`: a Pull Request labelled `DO NOT MERGE` is merged by nobody, including the approver whose merge the workflow otherwise expects, and the labels are read immediately before any merge rather than remembered from the review. The label itself did not exist and was created. The rule was placed where it would merge cleanly with `lab3-staging`, and that was checked rather than hoped. |
| 46 | *"ทำ #53 ต่อเลย"*<br>"Carry on with #53." | `lab3-staging` merged in, with conflicts resolved by meaning rather than by side: the error codes and styles #48 retired stayed retired while #53's were kept, test rows were merged by identifier keeping whichever had advanced, and the prompt log was renumbered into one sequence. Then the part #53 had deliberately left for after #48: the route, a role limit on the guard, and navigation that shows User Management only to an Administrator. |
| 47 | `/code-review low` | Run on #61 before review: no issues, with the reviewer stating what it read closely (server, API callers) and what it skimmed (screens, CSS). |
| 48 | *"https://github.com/beambeambeam/toktickit/pulls #63 #64 #65 #66 #67 รีวิวให้บีมหน่อยตามลำดับใช้ code-review caveman"*<br>"Review Beam's PRs #63–#67, in order, with caveman code review." | Five reviews in one terse format. The one real bug was in #63 — Save Owner unassigned a ticket when the dropdown was left untouched. The most useful cross-cutting check could only be done across the batch: lock ordering between five PRs that will share one database, traced for deadlocks and found clean. Two verdicts were left as Comment so that approving did not quietly commit to merging five overlapping PRs. |
| 49 | *"https://github.com/Kiatisakk/toktickit/pull/61 บีมรีวิวแล้ว"*<br>"Beam has reviewed PR #61." | Four findings, each checked against the code and, for the one about filters, against the handout itself rather than against the repository's own paraphrase of it. Three were real and got regression tests that were watched failing on the old screen first. The fourth exposed a sentence in `ui-spec.md` that claimed the handout excluded something it only calls not required — so the reply disagreed with the finding and the specification was still corrected. |
| 50 | *"beam merge ล่ะ"*<br>"Beam has merged." | #61 merged; #53 closed by hand and moved to Done. How #61 ended is owed to `reviewer.md` on the next feature branch, which this is. |
| 51 | *"ทำ #50 ต่อเลย"*<br>"Carry on with #50." | The staff Ticket Queue. Three things the contract did not settle were settled in the open rather than guessed: there was no endpoint to list the owners the Owner filter must offer, so one was added to the contract; the "problem appears resolved" field has no column until #49, so the contract now says it arrives there; and search turned out to be a wildcard match — a finding from reviewing the peer's PR, verified against PostgreSQL there and fixed here for both lists (D-19). The escaping test was confirmed by removing the escaping and watching two tests fail. |
| 52 | *"มันสามารถทำ issue แบบเยอะ ๆ เลยได้มั้ย"*<br>"Can many Issues be done at once?" | Yes, in separate worktrees, but the Issues' own *Blocked by* lines allowed only two at that moment: #49, and #51 if it branched from #50's unmerged work. The costs were stated rather than hidden — both change `schema.prisma` and the same four documents, so the second to merge absorbs the conflicts, and two large Pull Requests land on one reviewer at once. Nothing was started in parallel. |
| 53 | *"issue ล่าสุดคืออันไหน"*<br>"Which Issue is the latest?" | Two answers, kept apart: the last Issue *created* is #55, and the last one *worked* is #50, in review as #62. |
| 54 | *"ทำไมทำ 50 ก่อน 49"*<br>"Why do #50 before #49?" | No technical reason. #50 was recommended because it starts the longest chain (#50 → #51 → #52), but the order agreed at the start put #49 first, and the recommendation did not say it departed from that. It should have. Recorded here because it is a model-behaviour miss, not a code one. |
| 55 | *"แล้วต่อไปควรอะไร"*<br>"What should come next?" | #49: blocked by nothing, returns the sprint to the agreed order, and does not touch #50's code. Checked first that none of its rules needs a status #51 has not added yet. |
| 56 | *"ทำ #49 ต่อเลย"*<br>"Carry on with #49." | Public Comments and the resolved indication. The development database already carried #50's migration from the other branch, so `migrate dev` asked for a reset; instead of resetting, the SQL was generated by diffing the staging schema against the new one and applied with `migrate deploy`. The API suite was written after the routes, so it was mutation-checked — set-once removed, role guard removed, author read from the body — and each broke exactly the test meant to catch it; the screen's suite likewise against four broken rules. Three Lab 2 detail tests changed only as far as the new sections required. One timing flake under the full parallel run was met with a longer wait in that file alone, and the reason written beside it. |
| 57 | *"https://github.com/Kiatisakk/toktickit/pull/62 บีมรีวิวแล้ว"*<br>"Beam has reviewed PR #62." | Three findings, each checked against My Tickets before acting: its category failure handling proved the second right, and its five-column grid the third. A regression test for each, written and seen to fail first. The layout fix was then measured in a real browser, because jsdom's class-name assertion proves only that a class is there. The first finding was the defect I had raised in review of his queue that same day, and that is recorded rather than smoothed over. |
| 58 | *"https://github.com/Kiatisakk/toktickit/pull/62 beam review"*<br>"Beam has reviewed #62." | He had not yet: the reviews, comments and timeline held nothing of his after the changes he requested. Reported as found, with the likely reason (an unsubmitted pending review), rather than treated as an approval that was not there. |
| 59 | *"https://github.com/Kiatisakk/toktickit/pull/63 บีมรีวิวล่ะแก้ conflict ด้วย"*<br>"Beam has reviewed #63 — fix the conflict too." | #62 had merged in the meantime, so first the owed housekeeping: #50 closed by hand and moved to Done. The conflict was resolved by meaning, keeping both sides of three appended lists. Both findings were real, and one overturned a choice I had defended in a code comment: falling back to the local clock, defended there as better than reporting a failure, showed a time the server never recorded. Regression tests were seen failing first. The queue field #62's contract promised to this PR was added once #62 was in. |
| 60 | *"beam merge ล่ะ"*<br>"Beam has merged." | #63 merged; #49 closed by hand and moved to Done. How it ended is owed to `reviewer.md` on the next feature branch, which this is. |
| 61 | *"start"* | #51: the staff ticket operations. The status rename was done as a rename of the enum *value*, not an add-and-drop, so existing rows carried over and the lifecycle kept its order — the queue sorts on that order. Two Lab 2 tests asserted what this issue deliberately changes and were updated, with the reason written beside them. The endpoint tests were written after the code, so three rules were broken to check them: the transition matrix, owner eligibility, and IT Priority writing the Requester's column. The client needs its own copy of the matrix, because no endpoint hands one over; both copies are asserted against §5's table and the module says which one wins. |
| 62 | *"beam merge ล่ะ ทำ issue ต่อไปทิ้งไว้ได้เลยโดย SpawnSubAgent Sonnect 5 ถ้างานไว้โดยถ้าหากว่าเปิด PR แบบหลายอันได้แล้วไม่ Conflict ก็ทำด้วย ๆ"*<br>"Beam merged. Carry on with the next Issue in the background with a Sonnet 5 subagent; if several PRs can be opened without conflict, do those too." | #64 merged; #51 closed by hand and moved to Done. Checked which Issues that actually unblocked: #52 was the only one — #54 waits on #52, and #55 on #54 — so one subagent was spawned rather than several, since there was exactly one PR to open, not many that could run in parallel without conflict. It built #52, Internal Notes: a table of its own beside PublicComment (D-09), staff-only routes guarded so the role check answers before the ticket lookup — which is what makes a Requester's refusal identical whether the ticket is theirs, someone else's, has notes, or none (BR-32) — and an Internal Notes section on the shared Ticket Detail screen, tinted and lock-iconed, rendered only for staff and never for a Requester. |
| 63 | *"continue then start next issue"* | #65 merged; #52 closed by hand and moved to Done. Checked which Issues that actually unblocked: #54 was the only one — #55 depends on it — so one agent was spawned rather than several. It built #54, end-to-end journeys and visual evidence: `e2e/lab-03/authentication.spec.ts`, `staff-ticket-flow.spec.ts`, `user-administration.spec.ts` and `visual.spec.ts`, covering the 45-state, five-folder screenshot inventory `ui-spec.md` §11 names and RESP-01 to RESP-06. Running the suite twice found two real defects rather than test flakiness, and both were fixed at the cause: the header overflowed 97px sideways at 768px because nothing collapsed it below the mobile toggle's own 767px breakpoint, and the busy button's spinner animation made a screenshot differ on every run until `page.screenshot`'s `animations: "disabled"` froze it. A separate, deliberate non-determinism was found and left as-is: every screenshot showing a real ticket varies by ticket number and timestamp run to run, exactly as Lab 2's own `my-tickets`/`create-ticket`/`ticket-detail` screenshots already do — confirmed by running Lab 2's own suite twice and finding all of them modified, so this is an inherited characteristic, not a regression, and is reported rather than silently excluded. |
| 64 | *"https://github.com/Kiatisakk/toktickit/pull/66 solve comment"*<br>"Resolve the review comments on PR #66." | Six findings, all real. Five were browser tests that could pass without proving their own name, and one was a leak of uploaded files. The widened touch-target check found a genuine 30 px brand link on mobile the moment it ran. That is a test that had been passing vacuously, and it is recorded as such. Before this, the orchestrating session had also found and fixed two things in the agents' work: a test-fixture type error of its own from #51 that broke `tsc -b`, and a reset script able to empty the development database. |
| 65 | *"Plan เลยต้องทำอะไรบ้าง"*<br>"Plan it — what needs doing?" | The closing plan for #55, made read-only first: 17 `Planned` rows with no implementation behind them (not forgotten Results but missing tests), a `style/` directory and `seed.api.test.ts` that did not exist at all, a Coverage table calling #66 Open after it merged, and a MIG-08 row contradicting the code. Three questions settled the order: AC-27 first, the reflection written by hand, the PDF last. |
| 66 | *"Implement Phase 1 & 2"* | The seventeen tests, in the order agreed. AC-27 first (API-31, MIG-04), then the API/SEC/MIG rows, then the ten style tests. Two things found while writing: `download.text` is undefined for binary bodies (supertest hands a Buffer — fixed in both suites that download), and the style directory needed `../../../src` imports one level deeper than every existing suite. MIG-08's row was rewritten rather than implemented as written: the code fails migrated accounts closed, it does not flag them, and the specification already says so. API-42's mutation check was run, not asserted from reading. |
| 67 | *"https://github.com/Kiatisakk/toktickit/pull/67"* | Checked the other session's answers to Beam's four findings instead of trusting them: ran the server suite Beam could not run (577 of 577), and found the real cause of the disputed finding — a bare `#57` autolinks to this repository, so a row about Beam's PR rendered as ours. The rows were correct, but the rendered page showed otherwise, so the references were fixed rather than argued. |
| 68 | *"อยากจะเขียน Reflect ว่า … เขียนลงใน ai-use.md แล้วก็ทำให้ reviewer.md จบพร้อม render"*<br>"I want to write my reflection — put it in ai-use.md, and make reviewer.md complete and ready to render." | The reflection went in as I dictated it: typos fixed, wording kept, and an English translation marked as one. §1 turned out to be false — it said no sub-agent had written a commit in this lab, after two had written #52 and #54 under an Opus 5 trailer — and was corrected. reviewer.md gained #67 and had #58 moved out from under the Coverage table, where it rendered as a stray line of pipes. |

## 3. My reflection

> _Left for me to write in my own words before submission, as §14 Part 4 requires. It is the
> one section of this document that cannot be delegated, so it is deliberately empty rather
> than drafted._
